---
title: isObject
description: "Determine if a value is an Object"
---

import { SourceLinkAndPreview } from '@/components/SourceLinkAndPreview'
import { TestingLinkAndPreview } from '@/components/TestingLinkAndPreview'

## Basic usage

Pass in a value and get a boolean telling you if the value is an Object.

```ts
import { isObject } from 'radash'

isObject('hello') // => false
isObject(['hello']) // => false
isObject(null) // => false
isObject({ say: 'hello' }) // => true
```

## Testing

<TestingLinkAndPreview module="typed" func="isObject" />

## Source

<SourceLinkAndPreview module="typed" func="isObject" />