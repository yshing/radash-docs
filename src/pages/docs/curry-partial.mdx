---
title: partial
description: Create a partial a function
---

import { SourceLinkAndPreview } from '@/components/SourceLinkAndPreview'
import { TestingLinkAndPreview } from '@/components/TestingLinkAndPreview'

## Basic usage

Create a partial function by providing some -- or all -- of the arguments the given function needs.

```ts
import { partial } from 'radash'

const add = (a: number, b: number) => a + b

const addFive = partial(add, 5)

addFive(2) // => 7
```

## Testing

<TestingLinkAndPreview module="curry" func="partial" />

## Source

<SourceLinkAndPreview module="curry" func="partial" />