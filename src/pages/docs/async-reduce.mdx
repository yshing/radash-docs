---
title: reduce
description: Reduce an array with an async function
---

import { SourceLinkAndPreview } from '@/components/SourceLinkAndPreview'
import { TestingLinkAndPreview } from '@/components/TestingLinkAndPreview'

## Basic usage

A reduce that handles callback functions that return a promise.

```ts
import { reduce } from 'radash'

const userIds = [1, 2, 3, 4]

const users = await reduce(userIds, async (acc, userId) => {
  const user = await api.users.find(userId)
  return {
    ...acc,
    [userId]: user
  }
}, {})
```

## Testing

<TestingLinkAndPreview module="async" func="reduce" />

## Source

<SourceLinkAndPreview module="async" func="reduce" />