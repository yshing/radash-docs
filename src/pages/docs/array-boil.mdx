---
title: boil
description: "Reduce a list of items down to one item"
---

import { SourceLinkAndPreview } from '@/components/SourceLinkAndPreview'
import { TestingLinkAndPreview } from '@/components/TestingLinkAndPreview'

## Basic usage

Given an array of items return the final item that wins the comparison condition. Useful for more complicated min/max.

```ts
import { boil } from 'radash'

const gods = [{
  name: 'Ra',
  power: 100
}, {
  name: 'Zeus',
  power: 98
}, {
  name: 'Loki',
  power: 72
}]

const mostPowerful = boil(gods, (a, b) => a.power > b.power ? a : b) // => { name: 'Ra', power: 100 }
```

## Testing

<TestingLinkAndPreview module="array" func="boil" />

## Source

<SourceLinkAndPreview module="array" func="boil" />