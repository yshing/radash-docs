---
title: first
description: Get the first item from a list
---

import { SourceLinkAndPreview } from '@/components/SourceLinkAndPreview'
import { TestingLinkAndPreview } from '@/components/TestingLinkAndPreview'

## Basic usage

Given an array of items return the first item or a default value if no items exists.

```ts
import { first } from 'radash'

const gods = ['ra', 'loki', 'zeus']

first(gods) // => 'ra'
first([], 'vishnu') // => 'vishnu' 
```

## Testing

<TestingLinkAndPreview module="array" func="first" />

## Source

<SourceLinkAndPreview module="array" func="first" />