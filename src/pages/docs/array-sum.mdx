---
title: sum
description: Add up all items of an array
---

import { SourceLinkAndPreview } from '@/components/SourceLinkAndPreview'
import { TestingLinkAndPreview } from '@/components/TestingLinkAndPreview'

## Basic usage

Given an array of items, and an optional function to map each item to a number, add up all the items.

```ts
import { sum } from 'radash'

const fish = [{
  name: 'Marlin',
  weight: 100
}, {
  name: 'Bass',
  weight: 10
}, {
  name: 'Trout',
  weight: 15
}]

const totalWeight = sum(fish, f => f.weight) // => 125
```

## Testing

<TestingLinkAndPreview module="array" func="sum" />

## Source

<SourceLinkAndPreview module="array" func="sum" />