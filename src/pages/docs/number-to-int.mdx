---
title: toInt
description: Convert a value to an int if possible
---

import { SourceLinkAndPreview } from '@/components/SourceLinkAndPreview'
import { TestingLinkAndPreview } from '@/components/TestingLinkAndPreview'

## Basic usage

The `_.toInt` function will do its best to convert the given value to an int.

```ts
import { toInt } from 'radash'

toInt(0) // => 0
toInt(null) // => 0
toInt(null, 3) // => 3
```

## Testing

<TestingLinkAndPreview module="number" func="toInt" />

## Source

<SourceLinkAndPreview module="number" func="toInt" />